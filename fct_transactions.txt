with fct_transactions as (
	SELECT 
		DENSE_RANK() OVER (ORDER BY transaction_id) trans_id,
		cust_id,
		order_id,
		prod_id,
		order_date,
		price,
		quantity
	FROM `my-dbt-customer-orders.salesDB.orders`	
)